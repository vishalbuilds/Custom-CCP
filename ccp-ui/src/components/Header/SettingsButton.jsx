
import { Settings } from 'lucide-react'

export default function SettingsButton() {
    return (<button className="p-2 hover:bg-white/10 rounded-lg transition-colors">
        <Settings size={22} />
    </button>)
}